import Typography from './Typography';

const Layout = () => {
  return (
    <header>
      <Typography handwriting component="h1">
        Sher
      </Typography>
    </header>
  );
};

export default Layout;
