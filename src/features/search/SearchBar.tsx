import ContainedTextInput from '../../components/TextInput/ContainedTextInput';

const SearchBar = () => {
  return <ContainedTextInput placeholder="szukaj wszędzie" />;
};

export default SearchBar;
